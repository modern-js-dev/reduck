export declare const ZH_LOCALE: {
  command: {
    new: {
      describe: string;
      debug: string;
      config: string;
      plugin: string;
      distTag: string;
      registry: string;
    };
  };
};